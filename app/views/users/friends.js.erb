$("<%= escape_javascript(render(partial: 'users/friends', :locals => {:friends => @friends, :user => @user })) %>")
.replaceAll("#app");

$(document).ready(toCreateCommentAction);
$(document).ready(sendMessage);
