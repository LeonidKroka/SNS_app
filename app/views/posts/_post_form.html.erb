<div class="post-forms">
  <%= form_for([@user, @post], remote: true) do |form| %>
    <%= form.label :text, hidden: :true %>
    <%= form.text_area :text, class: "post-create-body",
                              placeholder: "Write your new post",
                              autofocus: "true" %> <br>
    <%= button_tag(type: 'submit') do %>
      Send
    <% end %>
  <% end %>
</div>
